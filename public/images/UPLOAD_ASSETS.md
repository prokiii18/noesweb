Required before merging this branch: add `public/images/becherovka.webp` and `public/images/biolage.webp` (provided separately as `noes-portfolio-images.zip`). They must be real WebP files rather than text placeholders. Verify both the portfolio card and dialog in a browser. PR #3's asset checker currently targets the removed script and needs a follow-up adjustment after merging this image-fix PR.
